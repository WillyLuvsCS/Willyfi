<h1 class="text-3xl font-bold mb-6">🎵 Our Albums</h1>

<div class="grid grid-cols-1 md:grid-cols-3 gap-6">
  <% @albums.each do |album| %>
    <div class="bg-white shadow-lg rounded-xl overflow-hidden transition-transform hover:scale-105">
      <%= image_tag(album.image_url, class: "w-full h-60 object-cover") if album.image_url.present? %>
      <div class="p-4">
        <h2 class="text-xl font-semibold"><%= album.title %></h2>
        <p class="text-gray-600"><%= album.artist %></p>
      </div>
    </div>
  <% end %>
</div>
