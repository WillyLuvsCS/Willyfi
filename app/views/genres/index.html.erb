<h1>Genres</h1>

<table>
  <thead>
    <tr>
      <th>Select a Genre to Browse</th>
    </tr>
  </thead>
  <tbody>
    <% @genres.each do |genre| %>
      <tr>
        <td><%= link_to genre, artists_path(genre: genre) %></td>
      </tr>
    <% end %>
  </tbody>
</table>